* Regex
	var regex=/^[a-zA-Z]+$/;
    if (!x.match(regex))
    {
        alert("Must input string");
        return false;
    }


* get dateobject from combinations of month, year, date
https://www.w3schools.com/js/js_dates.asp

*ng generate module logout --route logout  --module app.module